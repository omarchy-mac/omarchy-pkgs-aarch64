diff --git a/pkgbuilds/omarchy-dev/PKGBUILD b/pkgbuilds/omarchy-dev/PKGBUILD
index 58630ed..73cd245 100644
--- a/pkgbuilds/omarchy-dev/PKGBUILD
+++ b/pkgbuilds/omarchy-dev/PKGBUILD
@@ -44,6 +44,7 @@ depends=(
 
   # System font (UI assumes it)
   'ttf-jetbrains-mono-nerd-basic'
+  'snapper' # Root snapshots also apply to btrfs installations on Apple Silicon.
 )
 
 # Bootloader stack. Lives here rather than on omarchy-settings because
@@ -57,16 +58,16 @@ depends_x86_64=(
   'limine'
   'limine-mkinitcpio-hook'
   'limine-snapper-sync'
-  'snapper'
 )
 
 # Apple Silicon boots through m1n1 + GRUB from the Asahi packages and keeps
-# Arch Linux ARM's kernel and boot layout, so the Limine/Snapper stack does not
+# Arch Linux ARM's kernel and boot layout, so the Limine stack does not
 # apply. Wi-Fi on the Broadcom parts only scans reliably through the iwd
 # backend, which install/hardware/network.sh selects on Apple Silicon.
 depends_aarch64=(
   'iwd'
   'networkmanager'
+  'zram-generator' # Reads the compressed-swap configuration shipped on Apple Silicon.
 )
 
 makedepends=(
diff --git a/pkgbuilds/omarchy-settings-dev/PKGBUILD b/pkgbuilds/omarchy-settings-dev/PKGBUILD
index 6c7f8d9..2cf5edb 100644
--- a/pkgbuilds/omarchy-settings-dev/PKGBUILD
+++ b/pkgbuilds/omarchy-settings-dev/PKGBUILD
@@ -193,15 +193,20 @@ package() {
   install -Dm644 default/systemd/user/omarchy-tailscale-receive.service "$pkgdir/usr/lib/systemd/user/omarchy-tailscale-receive.service"
   install -Dm644 default/systemd/user/omarchy-fcitx5.service "$pkgdir/usr/lib/systemd/user/omarchy-fcitx5.service"
   install -Dm644 default/systemd/user/omarchy-crash-watch.service "$pkgdir/usr/lib/systemd/user/omarchy-crash-watch.service"
+  # Newer source trees enable this at first login; older pinned releases do
+  # not contain or request the unit. Keep both source layouts buildable.
+  if [[ -f default/systemd/user/omarchy-brightness-keyboard-auto.service ]]; then
+    install -Dm644 default/systemd/user/omarchy-brightness-keyboard-auto.service "$pkgdir/usr/lib/systemd/user/omarchy-brightness-keyboard-auto.service"
+  fi
   # Marks app.slice (and only app.slice) as a systemd-oomd kill candidate, so
   # memory pressure costs one app scope instead of the compositor session.
-  # Thresholds live in etc/systemd/oomd.conf.d/10-omarchy.conf. Both this and
-  # the zram vendor drop-in belong to the x86_64 memory stack: the aarch64
-  # install neither enables systemd-oomd nor configures zram swap.
+  # Thresholds live in etc/systemd/oomd.conf.d/10-omarchy.conf.
   if [[ $CARCH == x86_64 ]]; then
     install -Dm644 default/systemd/user/app.slice.d/10-oomd.conf "$pkgdir/usr/lib/systemd/user/app.slice.d/10-oomd.conf"
-    install -Dm644 default/systemd/zram-generator.conf.d/90-omarchy.conf "$pkgdir/usr/lib/systemd/zram-generator.conf.d/90-omarchy.conf"
   fi
+  # Apple Silicon also configures compressed swap. A source-only copy under
+  # /usr/share/omarchy is invisible to zram-generator; keep its runtime file.
+  install -Dm644 default/systemd/zram-generator.conf.d/90-omarchy.conf "$pkgdir/usr/lib/systemd/zram-generator.conf.d/90-omarchy.conf"
   install -Dm644 default/systemd/system/plocate-updatedb.service.d/10-omarchy.conf "$pkgdir/usr/lib/systemd/system/plocate-updatedb.service.d/10-omarchy.conf"
 
   # Same for applications/** — used by omarchy-refresh-applications,
diff --git a/pkgbuilds/omarchy-settings/PKGBUILD b/pkgbuilds/omarchy-settings/PKGBUILD
index f466293..f0bc20a 100644
--- a/pkgbuilds/omarchy-settings/PKGBUILD
+++ b/pkgbuilds/omarchy-settings/PKGBUILD
@@ -188,15 +188,20 @@ package() {
   install -Dm644 default/systemd/user/omarchy-tailscale-receive.service "$pkgdir/usr/lib/systemd/user/omarchy-tailscale-receive.service"
   install -Dm644 default/systemd/user/omarchy-fcitx5.service "$pkgdir/usr/lib/systemd/user/omarchy-fcitx5.service"
   install -Dm644 default/systemd/user/omarchy-crash-watch.service "$pkgdir/usr/lib/systemd/user/omarchy-crash-watch.service"
+  # Newer source trees enable this at first login; older pinned releases do
+  # not contain or request the unit. Keep both source layouts buildable.
+  if [[ -f default/systemd/user/omarchy-brightness-keyboard-auto.service ]]; then
+    install -Dm644 default/systemd/user/omarchy-brightness-keyboard-auto.service "$pkgdir/usr/lib/systemd/user/omarchy-brightness-keyboard-auto.service"
+  fi
   # Marks app.slice (and only app.slice) as a systemd-oomd kill candidate, so
   # memory pressure costs one app scope instead of the compositor session.
-  # Thresholds live in etc/systemd/oomd.conf.d/10-omarchy.conf. Both this and
-  # the zram vendor drop-in belong to the x86_64 memory stack: the aarch64
-  # install neither enables systemd-oomd nor configures zram swap.
+  # Thresholds live in etc/systemd/oomd.conf.d/10-omarchy.conf.
   if [[ $CARCH == x86_64 ]]; then
     install -Dm644 default/systemd/user/app.slice.d/10-oomd.conf "$pkgdir/usr/lib/systemd/user/app.slice.d/10-oomd.conf"
-    install -Dm644 default/systemd/zram-generator.conf.d/90-omarchy.conf "$pkgdir/usr/lib/systemd/zram-generator.conf.d/90-omarchy.conf"
   fi
+  # Apple Silicon also configures compressed swap. A source-only copy under
+  # /usr/share/omarchy is invisible to zram-generator; keep its runtime file.
+  install -Dm644 default/systemd/zram-generator.conf.d/90-omarchy.conf "$pkgdir/usr/lib/systemd/zram-generator.conf.d/90-omarchy.conf"
   # Older release pins still configure locate through install/config/locate.sh.
   if [[ -f default/systemd/system/plocate-updatedb.service.d/10-omarchy.conf ]]; then
     install -Dm644 default/systemd/system/plocate-updatedb.service.d/10-omarchy.conf "$pkgdir/usr/lib/systemd/system/plocate-updatedb.service.d/10-omarchy.conf"
diff --git a/pkgbuilds/omarchy/PKGBUILD b/pkgbuilds/omarchy/PKGBUILD
index 2e20a38..95ea705 100644
--- a/pkgbuilds/omarchy/PKGBUILD
+++ b/pkgbuilds/omarchy/PKGBUILD
@@ -58,6 +58,7 @@ depends=(
 
   # System font (UI assumes it)
   'ttf-jetbrains-mono-nerd-basic'
+  'snapper' # Root snapshots also apply to btrfs installations on Apple Silicon.
 )
 
 # Bootloader stack. Lives here rather than on omarchy-settings because
@@ -71,16 +72,16 @@ depends_x86_64=(
   'limine'
   'limine-mkinitcpio-hook'
   'limine-snapper-sync'
-  'snapper'
 )
 
 # Apple Silicon boots through m1n1 + GRUB from the Asahi packages and keeps
-# Arch Linux ARM's kernel and boot layout, so the Limine/Snapper stack does not
+# Arch Linux ARM's kernel and boot layout, so the Limine stack does not
 # apply. Wi-Fi on the Broadcom parts only scans reliably through the iwd
 # backend, which install/hardware/network.sh selects on Apple Silicon.
 depends_aarch64=(
   'iwd'
   'networkmanager'
+  'zram-generator' # Reads the compressed-swap configuration shipped on Apple Silicon.
 )
 
 makedepends=(
