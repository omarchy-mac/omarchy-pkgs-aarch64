# Package encryption/Limine integration preparation

Base: tested nine-package recipe source `5b41ff12fbf97c7fc86a161e58763ebc97e4e2d9`. Upstream destination main was `78bf4a2d659187c10c722d8d0e8c41dd9e5e3726` at inventory time; compare its later changes separately before an eventual merge. Existing package worktrees are untouched.

Prepare a separate boot package from pinned `omarchy-mac-boot 20260921-9`, plus ARM64 `limine-mkinitcpio-hook 1.36.0-3`, `limine-snapper-sync 1.30.1-1`, `uboot-asahi 2026.07.asahi2-3`, and ARM64 settings-menu retention. The central inventory records source commits #155/#170/#182/#185/#186/#190. Resolve ALARM Limine and the full authenticated binary closure before admitting a build.

Keep `omarchy-mac` kernel-neutral. The proposed boot package owns conversion, vendor firmware/HID ordering, first-boot services and boot hooks, and replaces `omarchy-apple-boot`/`omarchy-first-boot`. Review every installed path and removal hook across the complete transaction, including the existing add-on/settings/runtime. The upstream first-boot script imports and locally signs Marcelo's key `C81AC3E2A99556F9B21D5FEA3DD49BC9F8360BDC`; exclude that key and adapt the lifecycle without changing target repository policy or installing candidate trust.

Extend `scripts/build-quattro-image-inputs.py`, signing/verification and the explicit candidate schema together with the image importer. Preserve source attribution, nine-package receipts, disjoint dependency snapshots, paired desktop/settings and immutable manifests. Boot tests to port are `test/omarchy-mac-boot`, `test/omarchy-mac-encrypt`, `test/omarchy-mac-first-boot`, and `test/omarchy-mac-hid-initramfs`; image activation checks belong to the image builder. Run focused fixture tests and `scripts/self-test.sh` after implementation, then validate ownership in a disposable full transaction. Do not alter edge publishing workflows or production trust.

This branch now contains the draft boot package, ARM64 Limine/U-Boot recipes and opt-in schema-4 candidate producer/signer. The initial checkpoint had no native package build or full installation transaction evidence; the later integration qualification is recorded below. The complete dependency inventory and 168-file disposition map are in the desktop repository, branch `integrate/quattro-encryption-limine`, under `docs/quattro-encryption-limine-{integration,sources,files}-2026-09-22.{md,json,json}` (three separate files). The local desktop worktree is `/home/scott/code/omarchy-worktrees/quattro-encryption-limine`.

The proposed runtime source ceiling is `maralcbr/omarchy-mx-mac` PR #220 at `d418ab7f95e8ba447df4fb368ddd838a5ffc7943`, including merged #219 at `5e7a409fae1ddc17433d9408e15153b4fe813f7b`. The package/image reference is `maralcbr/omarchy-pkgs` PR #194 at `68a61cef1aba768c6aae20a0feda2a42e19de6e8`. These heads were verified with GitHub API on September 22. Both PRs were subsequently observed merged on September 22 with these exact heads unchanged. Preserve pins and original attribution; merged source is not a qualified downloadable candidate.

Keep candidate packages/images private and signed under the existing build-input policy. Preserve the active desktop, unrelated dirty worktrees, installed-user feed and repository trust. No publication, remote update, physical disk operation or boot-policy change is part of this preparation.

## Source checkpoint

The imported payload is adapted from the pinned package head above. Exclusions: embedded `omarchy-arm-repository.key`, `mac-image-finalize` and its compatibility links, and the old GRUB snapshot-menu hook. First boot uses the distilled runtime Limine leaf and existing platform keyring population, with no added/locally signed repository certificate. `cryptsetup` is explicit. Recipe staging preserves symlinks; ARM64 settings-template retention is opt-in. Existing package workflows and the nine-package default are unchanged.

Run `bash test/boot-source` for offline payload, state/retry and firmware/HID checks. It explicitly disables Docker/block test lanes. These checks passed, as did 92 existing publisher self-tests, 16 candidate ownership checks and 13 signing checks (disposable fixture keys, both schemas). Exact pinned settings recipes were checked after their baseline preparation. At that source checkpoint, reencryption, initramfs assembly, native compilation and legacy-package replacement were unqualified. Later native and focused replacement evidence below does not qualify reencryption or a bootable image.

The image repository can authenticate schema 4 but refuses to assemble it until its dependency snapshot and finalization contract are integrated. A complete signed platform closure, build prerequisites (including the recipes' native build dependencies), first-boot/factory state and VM boot evidence are still required. Do not treat these source recipes as ready-to-install artifacts. See the central runtime `docs/quattro-encryption-limine-source-port-2026-09-22.md` for the remaining work and verification ledger.

## Review follow-up

The central runtime source-port record now explicitly names `/var/lib/omarchy/mac-first-boot/deferred-steps` (paired with `mac-first-boot/pending`) and `/var/lib/omarchy/limine.enabled` as missing builder outputs, plus the separate owner `provisioning/pending` marker. Their creation belongs to coordinated image finalization and artifact inspection, including factory-state cleanup; neither is supplied by importing the excluded monorepo helper or touching a placeholder. The schema-4 image guard remains in place and the default schema-3 image is unchanged.

The conversion hook has a documented local deviation: a Limine marker with missing defaults/updater fails before the first boot-file rewrite, and a failed updater cannot mark conversion configured. A device-free fixture covers both missing prerequisites, update failure, successful Limine completion and the plain GRUB path. Recipe metadata keeps literal versions for the candidate parser, but U-Boot's source tag is derived from that literal and the Limine entry recipe's unused duplicate version variables are removed. `cryptsetup` is an explicit dependency because the boot package owns encryption conversion.

## Package integration qualification

Implementation checkpoint: `c71c8c93180d05c1c96e6d560dcfb4b35c627bb6`. Dependency sealing and verification now accept an explicit Limine profile: schema 2, `boot_profile: limine`, `candidate_schema: 4`, and exactly fifteen exclusions (all thirteen candidate packages plus both replaced legacy boot packages). The schema-1 default and workflows are unchanged. First boot validates the builder's exact `install/hardware/apple/limine-boot.sh\n` deferred-step record while continuing to allow marker-free reset. It executes the fixed leaf, never a file-supplied path.

All four boot recipes compiled on native ARM64 in a disposable container limited to 6 GiB and two CPUs. Source and patch checksums passed, including the architecture-specific Gradle/GraalVM archives. The final package metadata and shell-hook changes were repackaged from those outputs; the compiled Java/U-Boot sources did not change. Declared make dependencies now cover U-Boot's Python/setuptools/SWIG/OpenSSL/GnuTLS host tooling and GraalVM's zlib linkage. Inspection of both native Limine binaries showed a dynamic dependency on `libz.so.1`, so both recipes now declare runtime `zlib` too. Schema-4 builds check their make dependencies before beginning the desktop suite.

The real pacman replacement transaction exposed another inherited defect: Limine reads its ESP defaults before executing boot pre-hooks, so the original opt-in gate could not prevent errors on an unactivated image. A second local Limine patch checks a package-owned readiness helper before that initialization. A dormant Apple install delegates the original target stream to stock mkinitcpio; dormant removal/deploy succeeds without editing Limine. An armed marker with missing or invalid defaults fails. Generic installations without the Apple helper retain the upstream path. The existing pre-hook uses the same readiness helper.

The focused replacement transaction then passed with real archives and package hooks in a disposable container: customized legacy configuration survives as `.pacsave`, new initcpio and first-boot gates retain correct ownership, pending migrates without granting fresh-image conversion, and old first-boot payload/key paths are removed. The transition and dormant install/deploy/removal paths report no hook errors. This fixture has no installed kernel or ESP; a separate run including U-Boot records the expected `update-m1n1` failure for that missing environment. Container setup also records an unrelated systemd modules-load failure separately from the successful reviewed transaction. Neither result qualifies the complete thirteen-package image transaction or boot behavior.

Evidence is retained at [`encryption-limine-packages-integration-20260922`](/home/scott/code/omarchy-integration-evidence/encryption-limine-packages-integration-20260922/evidence.json), including source recipes, native build logs, package archives, `.BUILDINFO`-bound recipe hashes, signature-verified ALARM Limine origin bytes, focused transaction logs, and exact file hashes. The six locally built archives include two legacy fixtures and are unsigned qualification artifacts, not signed candidate inputs or an installer for distribution. The ALARM Limine archive is `12.9.0-1`, SHA256 `abadb32ec9fb6dfa70ba224f3b90435b93ea39db91dc2e838862c620f33fd266`, authenticated by the existing ALARM keyring signer `68B3537F39A313B3E574D06777193F152BDBE6A6`; image-side pinning belongs to the separate Limine platform lock.

Validation passed: the offline boot-source suite including the new gate regression, 92 publisher self-tests, 17 candidate ownership/prerequisite tests, 13 candidate signing tests, and 7 dependency snapshot tests. Signing used disposable fixture keys only. The full schema-4 candidate, authenticated aggregate dependency transaction, finalized-image verification, VM conversion/second boot, and physical model qualification remain later coordinated boundaries. No workflows select schema 4 and no candidate/image guard is removed by this package work.
