post_install() {
  if usr/bin/pacman-key --list-keys >/dev/null 2>&1; then
    usr/bin/pacman-key --populate omarchy-mac
  else
    printf '%s\n' 'Initialize pacman trust, then run: pacman-key --populate omarchy-mac'
  fi
}
post_upgrade() { post_install; }
