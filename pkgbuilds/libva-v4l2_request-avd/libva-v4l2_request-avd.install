post_install() {
  cat <<'MSG'
==> This package installs itself as asahi_drv_video.so as well, which is the
    name libva looks for on Apple Silicon, so VA-API applications should pick
    the decoder up with no configuration.

    If something does not, select it explicitly for the session:

        LIBVA_DRIVER_NAME=v4l2_request

==> Check with:  vainfo
==> mpv:         mpv --hwdec=vaapi video.mp4
==> Firefox:     additionally needs MOZ_DISABLE_RDD_SANDBOX=1, because the RDD
                 sandbox blocks the /dev/video* and /dev/media* access this
                 driver requires. That weakens Firefox's media sandbox.
MSG
}

post_upgrade() {
  post_install
}
