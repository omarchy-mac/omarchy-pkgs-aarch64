# No login screen while first boot is pending, even once the installer has enabled
# SDDM, nor for the rest of a boot whose install replaced the kernel.
[Unit]
After=omarchy-mac-first-boot.service
ConditionPathExists=!/var/lib/omarchy/mac-first-boot/pending
ConditionPathExists=!/run/omarchy-first-boot/no-desktop
